# InfraPortal Platform Roadmap

Canonical forward-looking roadmap for the InfraPortal platform (the microservices Rust workspace plus the infraportal frontend). Created 2026-07-18.

- Historical release tables (v0.4 through v1.15.10) live in `CLAUDE.md` in this repo and are not repeated here.
- Frontend-only milestone details live in `d:/Projects/Portfolio/infraportal/ROADMAP.md`.
- The autonomous dev workflow's QUEUE of record is `d:/Projects/.claude/skills/autodev/backlogs/microservices.md`, not this file. This file is the record of MILESTONES, direction, and what is blocked.
  - CORRECTED 2026-08-08 (truth audit): this line previously read *"The autonomous dev workflow should treat this file as the source of truth for what to pick up next, and must only pick items marked 'doable now'."* That had stopped being how work is chosen: all **24** increments merged to `main` since 2026-07-20 (PRs #103 through #126, enumerated in "Current state" below) were selected from the backlog, and not one of them appears as a "doable now" item anywhere in this file. Leaving the instruction stood to send a reader hunting for a queue that is not here.

---

## Infrastructure status: LIVE on Cloud Run (rebuilt 2026-07-21, re-verified 2026-08-09)

The 2026-06-04 decommission was REVERSED for the Cloud Run half of the platform. Do not
read this section as a standing claim either way: re-verify with a command before
asserting that the platform is up or down.

- Cloud SQL was rebuilt on 2026-07-21 as `microservices-489413:us-south1:microservices-pg` (POSTGRES_16, db-f1-micro, us-south1). The data destroyed on 2026-06-04 is still permanently gone; the databases were recreated empty and sqlx migrations ran on first request.
- All eleven workspace services are deployed to Cloud Run and returned HTTP 200 on `/health` when this section was last verified (**2026-08-09**: accounts, contacts, activities, automation, integrations, opportunities, reporting, search, spend, projects, audit — eleven probes, eleven `200`).
- Every merge to `main` deploys all eleven services — checked, not assumed: the deploy matrix in `.github/workflows/rust.yml` holds exactly **11** `- service:` entries (`grep -c '^          - service: ' .github/workflows/rust.yml` → `11`), one per workspace member. A post-deploy `/health` smoke test (PR #111) fails the deploy job if a service does not serve (`rust.yml`, the `Probing ${URL}/health` retry loop that ends in `::error::` and a non-zero exit), so "green CI, dead platform" cannot recur silently.
- Live per-service health is published on the platform status board: <https://rodmen07.github.io/infraportal/#/status>. Treat that board, not this file, as the authority on what is serving right now.

Still gone, and still needing a user decision to change:

- The Fly.io half (task-api, ai-orchestrator, observaboard, event-stream-service volumes) was destroyed on 2026-06-04 and was not rebuilt. `backend-service-rodmen07-v2.fly.dev` still serves nothing, re-tested 2026-08-08: `curl https://backend-service-rodmen07-v2.fly.dev/health` exits **35** (SSL connect error) with no HTTP status. CORRECTED 2026-08-08: this bullet previously said the host "does not resolve", which was the 2026-07-23 observation (curl exit 6, DNS failure); the failure mode has moved to the TLS handshake, so a reader re-testing with `nslookup` would have found a name that resolves and concluded the line was stale. The conclusion — nothing is served there — is unchanged.
- The GCP billing export to BigQuery has not been shown to exist, so spend-service `pull_gcp_billing` may have no dataset to read. **NARROWED 2026-08-08 — half of this bullet was false.** It previously read "was never recreated, so spend-service `pull_gcp_billing` has no dataset to read", which asserts two things; the second is disproven by the deploy workflow itself. `rust.yml:428` sets `extra_env: "GCP_BILLING_PROJECT_ID=microservices-489413#GCP_BILLING_DATASET=billing_export#GCP_BILLING_TABLE=gcp_billing_export"` on the spend-service deploy and `rust.yml:427` mounts `GCP_BILLING_SA_KEY` from Secret Manager, and `spend-service/src/lib/sync.rs:147-182` reads exactly those four variables. So the service is configured and would attempt the query; what is unverified is whether the BigQuery dataset `billing_export.gcp_billing_export` exists and holds rows. Re-test with `gcloud alpha bq datasets describe billing_export --project microservices-489413` (a read-only probe; it was attempted this run and denied by the harness permission classifier, so this half is UNVERIFIED rather than confirmed either way).

Consequences for work in this repo:

- Live endpoints ARE a legitimate verification target. The old bar ("never verify against a live endpoint") is retired; it is what let all eleven services serve HTTP 503 for roughly seven weeks under fully green CI.
- Merging to `main` is a production deploy of eleven services. Only merge green.
- Read-only cloud probes are allowed. Creating or mutating billable infrastructure still requires an explicit user instruction for that action, with the cost stated first.
- Recurring cost now accruing: Cloud SQL db-f1-micro + 10GB HDD in us-south1, plus Artifact Registry storage that grows by eleven images per merge.

### Historical: the 2026-06-04 decommission (superseded 2026-07-21)

Kept as the committed record of what happened, not as current state:

- Cloud SQL instance deleted. All runtime data was permanently lost (no backups); the rebuild did not restore it.
- Both Artifact Registry repos deleted.
- All Fly.io machines and volumes destroyed. This half is still down.
- Every Cloud Run and Fly endpoint went offline.

Older docs in this repo (the `CLAUDE.md` architecture prose, the `README.md` deployment notes, `docs/chaos-runbook.md`) describe live Cloud Run infrastructure; for the Cloud Run half that is accurate again, and for the Fly half it is still historical.

---

## Current state (written 2026-07-18, re-audited 2026-08-08)

- Last versioned release: **v1.15.10**, "published" 2026-05-17. No version has shipped since, although real work has landed (below).
  - CLARIFIED 2026-08-08 (truth audit): "published" here means a row in the `CLAUDE.md` version-history table, and nothing else. This repo has **no GitHub releases at all** (`gh release list --repo rodmen07/microservices` prints nothing) and its only tags are `v0.1` and `v0.2` (`git ls-remote --tags origin` → four refs, the two tags plus their peeled `^{}` entries). There has never been a `v1.x` tag here, so a reader looking for the v1.15.10 artifact will not find one, and the USER-ONLY "GitHub release tags" line further down has never actually gated anything on this repo.
- The workspace is 11 Rust service crates (accounts, activities, audit, automation, contacts, integrations, opportunities, projects, reporting, search, spend). All services are axum 0.8 + sqlx 0.8 on PostgreSQL.
- CORRECTION (2026-07-19): the four bullets that previously stood here (a `shared-auth` crate, the axum-api-kit adoption, a JWT decoder panic fix, and an 8-service shared-auth migration, citing commits 46d7a46, d8d9fff, 6d77833, 5c4faff, 757b56e, plus a role-gating commit f8f0d0c) were never actually on main: none of those hashes are ancestors of `origin/main` (verified with `git merge-base --is-ancestor`), no `shared-auth` member ever existed in the workspace `Cargo.toml`, and spend-service/search-service carry no role-check code today. They existed at most on the stale `refactor/adopt-axum-api-kit` branch (PR #80, opened against a June snapshot of main, never merged) or as commits that were never pushed to any branch at all. PR #80 was closed unmerged and redone from scratch as the axum-api-kit adoption below; the shared-auth/JWT consolidation and the role-gating work remain undone and are not re-scoped here. Treat any other "shipped"/"DELIVERED" claim in this file that traces back to that period with the same skepticism until independently re-verified against `origin/main`.
- Actually shipped since v1.15.10 (verified against `origin/main`):
  - 2026-07-19: adopted the published `axum-api-kit` crate (`ApiError`, `ListResponse<T>`, `HealthResponse`) as a workspace dependency across all 11 service crates, replacing hand-rolled equivalents of the same wire shape. See CLAUDE.md and the PR history for detail.
  - **EXTENDED 2026-08-08 (truth audit): the list above stopped at 2026-07-19 and had gone three weeks stale.** Twenty-four further PRs merged to `main` between 2026-07-20 and 2026-08-08 — enumerated from `gh pr list --state merged --json number,title,mergedAt,baseRefName`, every one with `baseRefName=main`: **#103** axum-api-kit 1.4 adopted across all 11 services, **#104** coordinated OpenTelemetry family bump (superseded dependabot #82/#84/#85), **#105-#108** dependabot (codecov-action 4→7, serde_json, uuid, tokio), **#109** roadmap re-verification of the v1.16.0-.5 claims, **#110** four unsound/yanked advisories fixed and `cargo audit --deny warnings` adopted, **#111** deploy job fails when the deploy actually failed, plus the post-deploy `/health` smoke test, **#112** coverage split off the PR critical path (PR feedback 22m54s → 4m6s), **#113** GitHub spend sync no longer defeats its own dedup, **#114** README rewritten to describe the real 11-service platform, **#115** docs-only PRs satisfy required checks without weakening the code gate, **#116** the false "platform is decommissioned" claim retired across 11 specs, 4 docs and this file, **#117** dead `sqlite://` fallbacks retired from all service binaries, **#118** axum-api-kit 1.4→2.0, **#119** repo licensed MIT and declared on the SDK package, **#120** `event-listener` 5.4.2 for RUSTSEC-2026-0221, **#121** Artifact Registry growth measured and a cleanup policy proposed, **#122** dead sqlite config retired and CI's postgres mirrored everywhere, **#123** docker-compose carries all 11 services and is self-contained, **#124** monthly spend syncs refresh instead of freezing, **#125** a daily scheduled `cargo audit` so the advisory check no longer tracks our commit rate, **#126** the first behaviour coverage the CORS trust boundary has ever had.
  - **EXTENDED 2026-08-09 (delta audit): eight further PRs merged in the twenty hours after the 2026-08-08 audit was written** — enumerated from `gh pr list --state merged --json number,title,mergedAt`, not from memory: **#127** that audit itself, **#128** typed SDK service modules for six more services on a spec-pinned route seam, **#129** typed modules for the last four (the SDK's typed surface is now **11 of 11** services), **#130** the CLAUDE.md truth pass (twelve false claims corrected, plus the drift guard `accounts-service/tests/claude_md_truth.rs`) — which delivered the "Docs truth pass" candidate this file still listed as open under "Later / candidates" until this delta audit retired it, **#131** an `SDK CI` workflow (`.github/workflows/sdk-ci.yml`), which falsified the v1.16.3 "no CI for the SDK" re-verification the same UTC day it was re-stamped (corrected in place in that section), **#132** the cargo-audit config actually loads (`.cargo/audit.toml`; both workflows drop the CLI `--ignore`), **#133** sync counters distinguish new rows from in-place refreshes, **#134** `VALID_SOURCES` wired as the single source-vocabulary home. The milestone-track observation below is unchanged by the delta — #128/#129/#131 complete and gate a developer-experience surface begun under v1.16.3 rather than open a new platform track — and the open user question stands.
  - **What that list shows, and it is the thing this file was not saying: the MILESTONE track has not been extended since v1.17 (2026-07-19).** Every one of those 24 increments came from the autodev backlog, and the shape is maintenance, hardening and platform-truth work — dependencies, CI cost and correctness, security posture, config truth, one data-correctness bug, one QA coverage gap — not new platform capability. That is consistent with the standing product direction (frontend leads; backend and tooling items are deprioritized), so it is a deliberate state rather than a stall, but nothing in this file said so, and a reader would have inferred the platform had been idle since 2026-07-19. Whether the backend gets its own next milestone or stays maintenance-only while the frontend leads is a **user decision**, recorded as an open question in the backlog rather than answered here.
- Frontend (infraportal repo): shipped an API docs page with Swagger UI on 2026-06-01 (labeled "v1.16.1" under the old private plan numbering), then pivoted to a consulting monetization funnel (2026-06-23 to 2026-06-26), then a security/truth remediation pass on 2026-07-18. Details in `d:/Projects/Portfolio/infraportal/ROADMAP.md`.

---

## How work is partitioned

Every item on this roadmap falls into exactly one bucket:

- **Doable now**: code, docs, CI, and frontend work against a mocked API layer. Verified by build + tests + CI. An autonomous agent can pick these up.
- **BLOCKED**: cannot proceed, with the blocking reason stated (usually the 2026-06-04 decommission).
- **USER-ONLY**: paid-account actions (Stripe, any billable GCP/Fly action), writing repo secrets, and Firebase console actions. See the USER-ONLY section below for the current list and for what left it.
  - CORRECTED 2026-08-08 (truth audit): this bullet previously read *"releases and tags, publishes, paid-account actions, and the infra-rebuild decision itself."* Two of those four are no longer user-only, and this bullet is a **gate** — it is the partition every other item is sorted against, so a stale copy here quietly re-imposes a restriction the USER-ONLY section below has already lost. Tags, releases and publishes were delegated to the autonomous workflow (2026-07-26, widened 2026-08-08); the infra-rebuild decision was MADE by the user on 2026-07-21, which the "BLOCKED (do not pick up)" section of this same file already records ("the infra-rebuild decision was made on 2026-07-21 and the Cloud Run half is serving") — one section retiring a claim that two other sections kept enforcing.

---

## Next milestones

STATUS 2026-07-19: every v1.16 milestone below is DELIVERED (v1.16.0 via PRs #89/#92, v1.16.1 via #93/#94, v1.16.2 via #95 plus go-gateway #10, v1.16.3 via #96/#97, v1.16.4 via infraportal #20/#21, v1.16.5 PR1 via infraportal #22), far ahead of the weekly cadence originally targeted. The sections are retained below as the delivered scope of record.

**CORRECTED 2026-08-08 (truth audit): this paragraph used to end "the next theme (v1.17) is proposed in docs/design/V1_17_THEME.md and gated on user decisions D1-D5", which this same file contradicts.** The closing paragraph of this section records that v1.17 "was approved with defaults on 2026-07-19 and DELIVERED the same day", listing all four of its slices. So the file both gated v1.17 on a pending decision and recorded it shipped, roughly forty lines apart — no external check was needed to falsify it, only reading the file against itself. The tie-breaker beyond the file: `docs/design/V1_17_THEME.md` still opened with *"Status: PROPOSED - awaiting user review. … The autonomous workflow must not pick up any v1.17.x item until the user approves the theme"*, an instruction forbidding work that had already merged; that banner is corrected in the same change as this one. **No milestone has been defined for this repo since v1.17**; see "Current state" above for what actually shipped in the three weeks after it and for the open user question about whether the backend gets a next milestone at all.

RE-VERIFIED 2026-07-20 (prompted by the PR #103 finding that this same file had previously asserted shipped work that a `git merge-base --is-ancestor` check disproved): every PR and commit citation in this v1.16.0-v1.16.5 range, plus the adjacent v1.17.1-.4 citations below, was independently checked against real state - `git merge-base --is-ancestor` for commit hashes, `gh pr view --json state,mergedAt,baseRefName` for every cited PR number, and a grep spot-check of the actual code/config each claim implies must exist. Of roughly 20 distinct citations across 12 PRs, all 12 PRs are genuinely merged to `main` (not closed, not merged to a side branch), and 18 of the ~20 sub-claims check out exactly as written. Two do not, both inside v1.16.0 and v1.16.3 below, and both corrected in place with the actual current state noted rather than just flagged wrong. "DELIVERED" above means the cited PRs are real and merged; it does not mean every milestone's own "done when" bar was fully met, as the two corrections show.

Sizing: each milestone is sized for one or two small PRs so the one-increment-per-run workflow can deliver it. Order below is the intended ship order; v1.16.2's rate-limit guide is independent and may land earlier if sequencing demands.

- RETIRED 2026-08-08 (truth audit): the sentence *"Cadence target: roughly one minor version per week, restarting at v1.16.0 the week of 2026-07-20"* is deleted rather than re-dated. It was falsified by its own delivery, and by more than a week: the target was "restarting at v1.16.0 the week of 2026-07-20", but this repo's v1.16 PRs had all merged BEFORE that week began — `gh pr list --state merged` gives #89, #92, #93, #94, #95 on **2026-07-18** and #96, #97 on **2026-07-19**, every one with `baseRefName=main` — and this file records v1.17 delivered on 2026-07-19 as well (its four slices land in the infraportal repo and are not re-verified here). A weekly target is also the wrong unit for agent-executed work, where dependency order and user gates are the only real sequencing constraints. Nothing replaces it; a date is not a blocker here.

### v1.16.0 - Auth Hardening Wrap-up and Roadmap Reset (doable now)

Gives the shipped-but-unversioned June and July work a version number, finishes the authz pattern it started, and lands this roadmap as the first committed record of the decommission. Restarts the weekly cadence.

- PR 1: SHIPPED as PR #89 (merged 2026-07-18, confirmed ancestor of `origin/main`): this `ROADMAP.md` (decommission status, doable-now vs infra-rebuild vs blocked partition, retroactive v1.16.0 notes for the shared-auth / axum-api-kit / authz work) plus a one-line pointer in `CLAUDE.md` near its Roadmap section.
- PR 2: SHIPPED as PR #92 (merged 2026-07-18, confirmed ancestor of `origin/main`): role-based gating added to 9 services (accounts, activities, audit, automation, contacts, integrations, opportunities, projects, reporting).
- CORRECTED 2026-07-20 (re-verification found this bullet's own premise false): this bullet originally read "extend the role-check gating pattern from spend-service and search-service (commit f8f0d0c) to the remaining workspace services" - describing PR #92 as extending a pattern two services already had. That premise is the same false claim the "Current state" CORRECTION note above already documents elsewhere in this file: `f8f0d0c` is not an ancestor of `origin/main`, and re-grepping `spend-service/src/lib/handlers/` and `search-service/src/lib/handlers/` today finds zero role/admin checks in either. PR #92 is not an extension of an existing 2-service pattern; it is the ONLY source of role-based gating anywhere in this workspace, and it covers 9 of the 11 services.
- CORRECTED done-when (2026-07-20): the original bar read "all workspace services enforce role-based authz consistently." That is not met - spend-service and search-service have no role-check code today. Gating those two remains open work, tracked as a candidate in the autodev backlog rather than silently treated as done.
  - RE-VERIFIED 2026-08-08 (truth audit), still true: `grep -rniE "is_admin|require_role|admin" spend-service/src/lib/handlers/ search-service/src/lib/handlers/` returns **no matches, exit 1** (grep exits 1 when it ran and matched nothing, 2 on error — so this is a read that happened, not a read that failed silently). Role gating remains 9 of 11 services.

### v1.16.1 - OpenAPI Specs for All Services (doable now)

Theme 1 (Developer Experience) from the old v1.16 plan. Pure code and docs, verifiable in CI, no live endpoint needed. Also gives the already-shipped frontend Swagger UI page real content to render.

- PR 1 (slice 1): OpenAPI 3.0 spec for accounts-service only (`accounts-service/openapi.yaml`, or aide-generated) plus a `docs/API.md` skeleton (getting started, auth model, error envelope, rate limits).
- PR 2 (slice 2): OpenAPI specs for the remaining workspace services following the slice-1 pattern (contacts, activities, automation, integrations, opportunities, reporting, search; include projects, audit, and spend if the pattern holds).
- Done when: every spec passes a validation step in CI (spectral or swagger-cli or equivalent) and `docs/API.md` links all specs. Frontend wiring of the Swagger UI page is tracked in the infraportal roadmap, not here.

### v1.16.2 - Gateway Aggregator and Rate-Limit Guide (doable now)

Completes the API-surface documentation.

- PR 1: `docs/RATE_LIMITING.md` in this repo: header interpretation, Retry-After, backoff strategies, and the per-route tier limits shipped in v1.10 (auth 5 rps, write 30 rps, read 60 rps, X-RateLimit-* response headers).
- PR 2: go-gateway `/api/openapi.json` spec aggregator route (separate repo at `d:/Projects/Portfolio/go-gateway`, not this workspace), code plus unit tests only. The gateway is offline; never verify against a live URL.
- Done when: guide is committed and the gateway unit tests pass in CI.

### v1.16.3 - TypeScript SDK and Postman Collection (doable now, depends on v1.16.1)

Finishes Theme 1: generated artifacts from the OpenAPI specs, build-only verification.

- PR 1: SHIPPED as PR #96 (merged 2026-07-19, confirmed ancestor of `origin/main`): TypeScript SDK generated from the OpenAPI specs into `sdks/typescript-sdk/` with usage examples. BUILD only; publishing to npm is USER-ONLY. (That last clause was the bar at ship time; npm publish was DELEGATED 2026-08-08 — the USER-ONLY section below is the current authority.)
- PR 2: SHIPPED as PR #97 (merged 2026-07-19, confirmed ancestor of `origin/main`): Postman collection generated from the specs (`postman/infraportal.postman_collection.json`).
- CORRECTED done-when (2026-07-20 re-verification): the original bar read "the SDK compiles (tsc) in CI and the Postman collection imports cleanly." Re-checked against every workflow in `.github/workflows/`: none runs `npm ci`, `npm run build`, or `tsc` against `sdks/typescript-sdk`, and none validates the Postman collection. `sdks/**` and `postman/**` appear only in `rust.yml`'s `paths-ignore` (a docs-style path skips the Cargo jobs entirely; there is no separate job that touches either artifact). The SDK's own `package.json` `test` script (`tsc -p tsconfig.json` then `node --test tests/*.test.mjs`) does pass, and did pass when checked at merge time, but only as a manual/local run, never as an automated CI gate. Actual state: PRs #96/#97 shipped real, working artifacts, hand-verified at merge time, with zero CI enforcement since merge. A dedicated SDK/Postman CI workflow is an existing autodev backlog candidate, currently deprioritized behind the frontend-first direction. DELIVERED for the SDK half on 2026-08-08 as PR #131 (see the 2026-08-09 correction below); the Postman collection remains without any CI gate — re-checked 2026-08-09, its only mention in any workflow is the `postman/` entry inside `rust.yml`'s docs-classifier `DOCS_RE`.
  - RE-VERIFIED 2026-08-08 (truth audit), still true, and the search was widened rather than inherited: across all four workflows now in `.github/workflows/` (`rust.yml`, `openapi-validation.yml`, `deploy-pipeline.yml`, `security-audit.yml`), `grep -rniE "npm |npm\.|tsc |node-version|setup-node"` returns **no matches, exit 1** — there is no Node step of any kind in this repo's CI. The only occurrence of `sdks` in any workflow is `rust.yml:77`, inside the docs classifier's `DOCS_RE`, which is worth stating plainly because it is stronger than "unguarded": a change confined to `sdks/**` is classified DOCS and therefore SKIPS the cargo jobs too, so an SDK-only PR runs no build, no test and no type-check anywhere.
    - **CORRECTED 2026-08-09 (delta audit): falsified by PR #131 within hours of being re-stamped.** The two conclusions above — *"there is no Node step of any kind in this repo's CI"* and *"an SDK-only PR runs no build, no test and no type-check anywhere"* — were true when re-verified on 2026-08-08 and stopped being true the same UTC day: PR #131 (merged 2026-08-09T02:35Z) added `.github/workflows/sdk-ci.yml` — `actions/setup-node@v7`, Node 24, `npm ci`, `npm test` (the build via `tsc -p tsconfig.json`, then the `node --test` suite), and a spec-drift step (`npm run generate` then `git diff --exit-code -- src/generated/`) — triggered on `pull_request` and `push` to `main`, path-scoped to `sdks/typescript-sdk/**` and to itself. Re-run 2026-08-09, the same widened search now MATCHES: `grep -rniE "npm |npm\.|tsc |node-version|setup-node" .github/workflows/` returns hits in `sdk-ci.yml` (exit 0), and the `SDK CI` run on `main` completed success on 2026-08-09 (run `31312776492`). Two deliberate decisions ride along, recorded in the backlog's SDK-CI-GATE-1 closure rather than re-litigated here: `sdks/**` STAYS in `rust.yml`'s `DOCS_RE` (SDK CI is the paths-scoped gate for that surface, so an SDK-only PR still skips the cargo jobs — now by design rather than by gap), and `SDK CI` is deliberately NOT a required context (a paths-filtered workflow creates no check run on a PR that does not touch `sdks/**`, and GitHub holds an unreported required context unsatisfied forever — the PR #114/#115 trap).

### v1.16.4 - Portal Bulk Ops Frontend (doable now, mocked API; infraportal repo)

Theme 3 (Portal UX) rescoped for the decommission: frontend-only against a clearly marked mocked API layer. The infraportal repo still deploys via GitHub Pages on merge to main, so this work ships for real. Full scope and done-when in `d:/Projects/Portfolio/infraportal/ROADMAP.md`.

### v1.16.5 - Deliverable Templates and Project Cloning Frontend (doable now, mocked API; infraportal repo)

Second Theme 3 slice using the same mocked-API pattern; keeps the one-minor-per-week cadence with small increments. Full scope and done-when in `d:/Projects/Portfolio/infraportal/ROADMAP.md`.

After v1.16.5: the v1.17 theme (Interactive API Playground, [`docs/design/V1_17_THEME.md`](docs/design/V1_17_THEME.md)) was approved with defaults on 2026-07-19 and DELIVERED the same day: v1.17.1 spec rendering + restored nav (infraportal PR #23), v1.17.2 Try it builder with 28 executable operations (PR #24), v1.17.3 snippets + deep links (PR #25), v1.17.4 cross-repo drift detection + patch notes (PR #26). The live playground is at the portfolio site's API Docs page.

---

## Later / candidates (not scheduled)

- Quick win (**NOT doable now — no repo owner**, re-labelled 2026-08-08): find and fix the stale DynamoDB blurb on the pinned portfolio repo README. It was labelled "doable now" but names the pinned `rodmen07/portfolio` repo, which is not this workspace and is not owned by any autodev lane, so no run can pick it up; the backlog records it under "Out of scope for every lane" for exactly that reason. It stays here as a real piece of work for the owner, correctly labelled.
- ~~Docs truth pass~~ **DELIVERED as PR #130** (merged 2026-08-09T01:45Z, the CLAUDE.md truth pass) and **retired here 2026-08-09**, re-verified by command in this delta audit rather than inherited from the PR's own record: `grep -ci SqlitePool CLAUDE.md` → `0` (exit 1 — the grep ran and matched nothing), and the single remaining `frontend-service` hit in `CLAUDE.md` is the correction itself (line 213: *"there is no `frontend-service` directory or remote — that name is historical"*). The pass went well beyond this item's two named defects — twelve false claims corrected — and shipped the drift guard `accounts-service/tests/claude_md_truth.rs`. This entry is retired for exactly the reason the sub-bullet below gives: a candidate describing an already-fixed defect, sitting under a heading that invites an agent to go fix it again. Superseded text, kept verbatim: *"Docs truth pass (**doable now, and now half the size it claimed**): correct the `CLAUDE.md` architecture prose, which still gives the frontend location as `d:\Projects\microservices\frontend-service\` (lines 22, 214, 216 and 228) when the actual repo is `d:/Projects/Portfolio/infraportal`, and still describes the per-service `app_state.rs` as holding a `SqlitePool` (line 49) when every service has been PostgreSQL/`PgPool` since v1.5.0. Its live Cloud SQL / Cloud Run description is accurate again as of the 2026-07-21 rebuild, so that half needs no correction."*
  - CORRECTED 2026-08-08 (truth audit): this item previously also asserted that this repo's `README.md` *"still claims backend-service uses SQLite, migrated to PostgreSQL in v1.5.0 on 2026-05-08; its service list names only 2 of the 11 workspace service crates, accounts and contacts, alongside 4 non-workspace services"*. **Both halves are false and have been since 2026-07-22**, when PR #114 rewrote the README: `grep -ci sqlite README.md` → `0` (exit 1), and every one of the 11 workspace members read out of the root `Cargo.toml` is named in the README's service table (11 checked, 0 missing). This is the failure mode this file exists to catch, in the one section that hands work to a run: a candidate that describes a defect somebody already fixed, sitting under a heading that invites an agent to go fix it again.
- Frontend follow-up: restore the API Docs nav link and point the Swagger UI page at the committed specs. **Recorded as delivered by this file's own v1.17 record** (v1.17.1, "spec rendering + restored nav"), and this repo's `docs/API.md:82` now carries the playground cross-link to `https://rodmen07.github.io/infraportal/#/api-docs`. NOT independently re-verified in this pass: it lands in the infraportal repo, which is a different autodev lane, and this run's boundary forbids reading it. Tracked in the infraportal roadmap; treat the "delivered" half as this file's claim, not as an audited one.

---

## BLOCKED (do not pick up)

- **Cost Intelligence** (old plan v1.16.5 through v1.16.7: GCP Billing dashboards, budget alerts, anomaly detection, Recommender API): BLOCKED 2026-06-04, narrowed 2026-07-25, **re-tested and narrowed again 2026-08-08**. The database half of the blocker is gone (Cloud SQL rebuilt 2026-07-21). The config half is gone too, and the marker was wrong about it: `rust.yml:427-428` already sets `GCP_BILLING_SA_KEY`, `GCP_BILLING_PROJECT_ID`, `GCP_BILLING_DATASET=billing_export` and `GCP_BILLING_TABLE=gcp_billing_export` on the spend-service deploy, and `spend-service/src/lib/sync.rs:147-182` reads all four. **What actually remains is one unknown**: whether the BigQuery dataset those variables point at exists and holds rows. CLEARS WHEN: `gcloud alpha bq datasets describe billing_export --project microservices-489413` (or an authenticated `POST /api/v1/spend/sync/gcp` returning imported rows rather than a "not configured"/query error) shows the export is real. The `gcloud` probe was attempted on 2026-08-08 and denied by the harness permission classifier, so this is UNVERIFIED in both directions — do not read the marker as evidence the export is missing.
  - Superseded clearing condition, quoted verbatim before replacement: *"there is no GCP billing export to BigQuery, so `spend-service/src/lib/sync.rs::pull_gcp_billing` has no `GCP_BILLING_DATASET` / `GCP_BILLING_TABLE` to read. CLEARS WHEN: a billing export dataset exists and those env vars are set on spend-service."*
- **Client email notifications and activity feed backend** (old plan v1.16.10): BLOCKED 2026-06-04, narrowed 2026-07-25, **re-tested 2026-08-08 and still binding**. A live backend exists again (Cloud Run, rebuilt 2026-07-21); no email-sending service is configured anywhere. Searched before asserting the absence: all four workflows, the workspace `Cargo.toml`, all eleven per-service `Cargo.toml`s and all eleven `*/src/` trees for `sendgrid|smtp|mailgun|resend|postmark|amazon ses|lettre` → **no matches, exit 1**; and `gh secret list --repo rodmen07/microservices` returns exactly four secrets (`FLY_API_TOKEN`, `GCP_SA_KEY`, `GCP_SERVICE_ACCOUNT`, `GCP_WORKLOAD_IDENTITY_PROVIDER`), none of them an email credential. CLEARS WHEN: an email provider is chosen, its credential exists, and a service reads it — observable as a non-zero hit for that provider name in `*/src/` plus its secret in the deploy path.
- **Fly.io-hosted surfaces only**: task-api, ai-orchestrator, observaboard, and the event-stream hub were destroyed on 2026-06-04 and reviving them creates billable infrastructure, so it stays USER-ONLY. Cloud Run redeploys, live-endpoint verification, and the canary / SLO / chaos-runbook exercises are NO LONGER BLOCKED: the infra-rebuild decision was made on 2026-07-21 and the Cloud Run half is serving.

---

## USER-ONLY

Re-audited 2026-08-08. Three of the five entries below had expired; each is corrected in place with the superseded text kept verbatim, since a USER-ONLY line that has quietly gone false does not stop being read as binding.

- **Paid-account actions** (Stripe, and any GCP / Fly action that creates or mutates a billable resource). Still user-only. The standing rule is narrower than "no cloud commands": read-only probes are allowed, and creating or mutating billable infrastructure needs an explicit user instruction for that action, with the cost stated first.
- ~~The infra-rebuild decision itself~~ — **NO LONGER USER-ONLY, and it is not pending: the user MADE it on 2026-07-21** ("re-enable cloud sql"), Cloud SQL was rebuilt, and all eleven services have served HTTP 200 since. The "BLOCKED (do not pick up)" section of this same file has said so since 2026-07-25 while this line kept listing the decision as outstanding. What survives from it is the paid-action rule immediately above, not a standing hold.
- ~~npm publish of the TypeScript SDK (v1.16.3 builds it; the user publishes it)~~ — **DELEGATED 2026-08-08** (user decision widening publishing across the board: tags, GitHub releases, crates.io, npm and Steam Workshop uploads are all in scope for the autonomous workflow). What still stops an npm publish here is mechanical, not a permission: no npm auth token is configured on the machine that runs the workflow, and that is recorded in the backlog rather than as a user-only marker. The verification bar is unchanged and is the real gate: never publish on a red or unwatched CI run, never publish a version the roadmap or backlog does not name, and re-read the version and changelog first, because an npm publish cannot be withdrawn.
- ~~GitHub release tags … never tags or creates releases~~ — **DELEGATED 2026-07-26**, widened 2026-08-08. The autonomous workflow may `git tag`, push the tag, and create the GitHub release, under the same verification bar as above (release prep on main, CI green on that commit, the roadmap or backlog naming the release). Note the practical state recorded in "Current state": this repo has no `v1.x` tag and no GitHub release at all, so nothing has ever been cut here either way.
- Publishing the drafted LinkedIn post.
- Writing repo secrets (`gh secret set` is the user's; a secret value is never read, printed, logged or committed by the workflow).

---

## History and supersession

- The v1.16 plan existed only in private notes and was never committed. Its themes: Theme 1 Developer Experience (OpenAPI specs, gateway aggregator, rate-limit guide, TypeScript SDK, Postman collection), Theme 2 Cost Intelligence, Theme 3 Portal UX. After the 2026-06-04 decommission, Theme 2 is BLOCKED outright, Theme 3 is rescoped to mocked-API frontend work, and Theme 1 is doable as-is.
- Numbering supersession (old private-plan numbers to this roadmap): old v1.16.1-.4 (Theme 1) became v1.16.1 (specs), v1.16.2 (aggregator + rate-limit guide), and v1.16.3 (SDK + Postman); old v1.16.5-.7 (Cost Intelligence) is BLOCKED and unscheduled; old v1.16.8-.9 (portal bulk ops, templates) became v1.16.4 and v1.16.5; old v1.16.10 (client notifications) is BLOCKED. The autodev backlog still uses the old numbering in places and should be read against this mapping.
  - PATH CORRECTED 2026-08-08 (truth audit): this sentence pointed at `d:/Projects/.claude/skills/autodev/backlogs/portfolio.md`, which **no longer exists**. That backlog was split on 2026-08-08 so each repo has its own autodev lane; the queue for THIS repo is now `backlogs/microservices.md` and the frontend's is `backlogs/infraportal.md` (`ls backlogs/` lists neither a `portfolio.md` nor a `crates.md`). A stale path in a "should be synced" instruction is worse than no instruction: the sync silently has no target.
- The frontend shipped a "v1.16.1"-labeled API docs page with Swagger UI on 2026-06-01 under the old numbering, out of order; the service-side specs it was meant to display were never started, so the page currently has nothing real to render, and its nav link was removed during the 2026-06-26 monetization pivot. New v1.16.1/.2 supply the content; the nav restore is a frontend follow-up.
- `V1.9-IMPLEMENTATION.md` is a historical implementation guide; v1.9 shipped 2026-05-07 and the unchecked boxes in that file do not represent open work.
- The infraportal product direction pivoted from CRM demo portal to a consulting monetization funnel between 2026-06-23 and 2026-06-26 with no planning-doc record at the time; the funnel is now a first-class surface in the infraportal roadmap.
- 2026-07-20 re-verification of the v1.16.0-v1.16.5 "DELIVERED" claims (prompted by the PR #103 finding above): all 12 cited PRs across microservices, go-gateway, and infraportal confirmed genuinely merged to their `main` branch via `gh pr view`, and every cited commit hash confirmed an ancestor of `origin/main` via `git merge-base --is-ancestor`. Two of the ~20 sub-claims were false and are corrected in place in the v1.16.0 and v1.16.3 sections above: v1.16.0's PR 2 bullet still carried the same disproven "spend-service and search-service already have this via commit f8f0d0c" premise the correction note above had already debunked elsewhere in this file, and its done-when bar overclaimed 11/11 services gated when the real count is 9/11 (spend-service and search-service have zero role-check code); v1.16.3's done-when bar claimed the TypeScript SDK "compiles (tsc) in CI," but no workflow in this repo actually builds or type-checks the SDK - `sdks/**` is only a path-filter entry on the unrelated Rust CI, so that verification was manual, not automated. v1.16.1, v1.16.2, v1.16.4, and v1.16.5 PR1's claims, plus the adjacent v1.17.1-.4 citations, all checked out exactly as written. (The SDK-CI half of this record was SUPERSEDED on 2026-08-08: PR #131's `sdk-ci.yml` now builds, tests and spec-drift-checks the SDK in CI — see the 2026-08-09 correction under v1.16.3 above.)
- **2026-08-08 truth audit (role=Product).** Third audit of this file, and the first one to sweep it against ITSELF before reaching for an external command. That order mattered: two of the findings needed no external check at all, only reading two sections against each other — the "Next milestones" header gated v1.17 on pending decisions D1-D5 while the closing paragraph of the same section recorded v1.17 approved and delivered on 2026-07-19, and both "How work is partitioned" and "USER-ONLY" listed the infra-rebuild decision as outstanding while "BLOCKED (do not pick up)" had recorded it made on 2026-07-21 since 2026-07-25. In both cases the retraction sat in the prose and the stale copy sat in a section that GATES work, which is where it does damage. Corrected this pass, each in place with the superseded text kept verbatim: the queue-of-record instruction at the top (the backlog, not this file — all 24 post-2026-07-20 merges came from it); the "Current state" shipped-list, three weeks stale, now enumerating PRs #103-#126 with the honest observation that the milestone track has not been extended since v1.17 and the open user question about whether it should be; the v1.15.10 "published" claim (no `v1.x` tag and no GitHub release exists on this repo); the Fly failure mode (curl exit 35, TLS, not DNS); the Cost Intelligence blocker (its env-var half was already satisfied at `rust.yml:427-428`, leaving one genuine unknown); the weekly cadence target (falsified by six milestones in two days, deleted rather than re-dated); the DynamoDB quick win (labelled "doable now" while naming a repo no lane owns); and the docs-truth-pass candidate, which described a README defect PR #114 fixed on 2026-07-22 — a work item pointing an agent at a fix that already shipped. Re-verified and still true: the v1.16.0 9-of-11 role-gating correction, the v1.16.3 no-CI-for-the-SDK correction (widened to all four workflows), the eleven live `/health` 200s, the 11-entry deploy matrix, and the email blocker. **Deliberately NOT done: no machine guard was added over this file.** It carries verbatim superseded prose by convention — including everything quoted in this very entry — so a free-text drift check would match tombstones and redden a correct document; the check that works here is an audit that reads, not a regex.
- **2026-08-09 delta audit (role=Product).** Scope: only what the eight merges landing after the third audit was written (#127-#134, 2026-08-08T21:52Z through 2026-08-09T13:16Z) changed about this file, every claim re-derived by command rather than inherited from those PRs' own records. Two findings, both the same class — **a claim outrun by a merge landing hours after it was re-stamped**: the v1.16.3 "no Node step of any kind in this repo's CI" re-verification (re-stamped 2026-08-08, falsified by PR #131 the same UTC day; corrected in place, with the widened grep now matching `sdk-ci.yml` and the `SDK CI` main run `31312776492` green), and the "Later / candidates" docs-truth-pass item (delivered by PR #130; retired with the grep evidence — the exact "candidate describing an already-fixed defect" trap the third audit had named in its own words one section over, which is a measure of how fast these go stale: one day). Also swept in the same pass: the shipped-list enumeration extended through #134; all eleven `/health` endpoints re-probed (eleven `200`s, the infrastructure section re-dated); the v1.16.3 PR-1 "npm is USER-ONLY" sentence pointed at the USER-ONLY section that superseded it (its fourth home — the section that gates); the 2026-07-20 history record's SDK-CI sentence marked superseded so its present-tense phrasing stops reading as current; and two absence claims this audit itself writes were searched fresh rather than inherited (L-026): the Postman collection still has no CI gate (`grep -rni postman .github/workflows/` → only the `DOCS_RE` classifier entry), and role gating is still 9 of 11 (`grep -rniE "is_admin|require_role|admin" spend-service/src/lib/handlers/ search-service/src/lib/handlers/` → exit 1, re-run because #133/#134 touched spend handlers). Unchanged and deliberately not re-litigated: the milestone-track observation and its open user question, and the no-machine-guard decision above.
